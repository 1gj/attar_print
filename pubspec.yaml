name: attar_print
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # النسخة الصحيحة والمستقرة حالياً
  blue_thermal_printer: ^1.2.3
  
  # باقي المكتبات
  screenshot: ^3.0.0
  intl: ^0.19.0
  permission_handler: ^11.3.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true